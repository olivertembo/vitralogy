import React from "react"
import Icon from "react-fa/lib/Icon"

const ChooseProof = () => (
  <div className="choose-proof text-center">
    <h1 className="mb-sm">
      <Icon name="arrow-circle-left" /> View Proof
    </h1>
    <p>Select a proof type to view its submission</p>
  </div>
)

export default ChooseProof
