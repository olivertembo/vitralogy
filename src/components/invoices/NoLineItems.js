import React from "react"

const NoLineItems = () => (
  <span className="meta line-item-loading">
    No line items for this date due to the applied filters above.
  </span>
)

export default NoLineItems
